#include "holberton.h"
/**
* rev_string - reverses a string
*
* @s: string to be reversed
*/
void rev_string(char *s)
{
int length = 0;
while (*s)
{
s++;
length++;
_putchar (*s);
}
while (length > 0)
{
s--;
length--;
_putchar (*s);
}
_putchar ('\n');
}
