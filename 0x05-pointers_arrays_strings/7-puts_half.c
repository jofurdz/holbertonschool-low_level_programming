#include "holberton.h"
/**
* puts_half - printing half of a string
*
* @str: string to be printed
*/
void puts_half(char *str)
{
int length = 0;
if (length % 2 == 0);
while (*str)
{
length++;
_putchar (*str);
}
}
