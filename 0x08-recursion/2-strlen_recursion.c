#include "holberton.h"
/**
 * _strlen_recursion - returns the length of a string
 *
 * @s: string to be printed
 *Return: 0
 */
int _strlen_recursion(char *s)
{
int rv;

for (rv = 0; s[rv] != 0; rv++)
{

}
return (rv);
}
