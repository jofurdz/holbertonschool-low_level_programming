#include "holberton.h"
/**
 * _puts_recursion - prints a string
 *
 * @s: char to be printed
 */
void _puts_recursion(char *s)
{
	int i = 0;

	while (s[i])
	{
		if (_putchar(s[i]) == '\0')
		{
			return ('\0');
		}
		i++;
	}
	if (_putchar('\n') == '\0')
}
