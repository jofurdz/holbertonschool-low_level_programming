#include "holberton.h"
/**
 *
 *
 *
 */
char *_strstr(char *haystack, char *needle)
{
size_t n = strlen(s2);
    while(*s1)
        if(!memcmp(s1++,s2,n))
            return (char *) (s1-1);
    return 0;
}
